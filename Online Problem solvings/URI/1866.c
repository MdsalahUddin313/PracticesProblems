
#include<stdio.h>
#include<math.h>
int main()
{
    int n,a,b=1,s,i;
    scanf("%d",&n);
    for(a=1; a<=n; a++)
    {
        scanf("%d",&s);
        for(i=1; i<=s; i++)
        {
            b=(-1*pow(1,2))+1;

        } printf("%d\n",b);
    }

    return 0;
}
